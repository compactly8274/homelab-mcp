"""Empty marker so homelab_mcp.scripts is a package.

The canary_cron module is invoked as
``python -m homelab_mcp.scripts.canary_cron`` from the system
cron. This __init__.py is the only thing making that path
importable.
"""
